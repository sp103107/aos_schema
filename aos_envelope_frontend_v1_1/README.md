# AoS / MCP Task Envelope Builder v1.1

Single-file static HTML app for creating AoS / MCP-compatible task envelopes.

- Dark / light mode toggle
- Mobile-first layout
- Creates JSON envelope with fixed schema
- Copy-to-clipboard and .json download
